# DataLab 全局 UI 验收清单

## 自动化验证

- [x] Debug 构建成功。
- [x] 现有 20 个 CTest 全部通过。
- [x] 分析设置弹窗、工作表模型、输出序列化和图形交互回归测试通过。

## 中文路径 Qt Creator 手工验收

- [ ] 启动后确认菜单栏、标准工具栏和状态栏层级清晰，窗口缩放到 1440×900 时无控件重叠。
- [ ] 在“查看”菜单中分别隐藏/显示项目导航器、上下文信息和活动工作表，确认中央输出区仍可用。
- [ ] 未导入数据时确认中央输出区显示空状态引导，右侧数据集状态为琥珀提示，并给出导入下一步。
- [ ] 导入中文路径 CSV/Excel 后确认项目导航器显示工作表，右侧显示行数、列数、排除状态，且下一步改为选择分析方法。
- [ ] 编辑、复制、粘贴、撤销和排除行，确认工作表列头、选中行、当前单元格地址（如 `C1 · 3`）和排除状态清楚；单击/双击单元格在格子内直接编辑，编辑器不撑大行高。
- [ ] 隐藏“活动工作表”后再显示，确认表格编辑仍可用。
- [ ] 运行描述统计、SPC、能力分析和图形分析，确认输出页标题、方法摘要、表格、图形和诊断按统一层级显示。
- [ ] 关闭一个输出页签，确认项目导航器同步移除该项；关闭全部页签后空状态重新出现。
- [ ] 已有输出时选择导入或新建，确认弹出丢弃确认；取消后现有结果保留。
- [ ] 在输出图形中验证悬停、缩放、拖拽、命中测试和刷选后工作表行联动。
- [ ] 图表左键双击不进入属性编辑；在绘图区右键菜单选择“编辑图形…”可进入属性页。
- [ ] 图表属性页在 1080×680、1440×900 和 125%/150% DPI 下无控件遮挡，底部按钮始终可见，数据系列列过窄时可横向滚动。
- [ ] Control、Scatter、Histogram、BoxPlot、Probability、Parallel、Contour、Marginal 图表悬停或点击图形对象时显示对应文字信息；缩放/平移后命中位置仍正确。
- [ ] XBAR 图表右键“编辑图形…”后在图表右侧展开属性面板；面板可拖动宽度、滚动、关闭，且不覆盖图表。
- [ ] XBAR 正常点、异常点、CL/LCL/UCL、数据区和空白区右键菜单显示相应编辑对象；悬停 tooltip 延迟出现且不遮住当前点。
- [ ] 选中 XBAR 点后显示高亮并联动工作表原始行；Ctrl/Shift 多选、框选、清除刷选保持可用。
- [ ] 在统计表中验证区域选择、TSV 复制和 CSV 导出只作用于当前输出对象。
- [ ] 双击输出页签修改标题，确认项目导航器同步更新。
- [ ] 打开分析设置弹窗，确认卡片布局、列类型、搜索、基础/高级参数、校验回填，以及整窗底部“重置默认值/取消/运行分析”保持可见。
- [ ] 导出 PDF/PNG 并重新打开项目，确认报告内容、中文标题和输出页索引不变。
