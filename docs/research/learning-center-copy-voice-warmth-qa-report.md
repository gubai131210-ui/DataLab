# 学习中心文案口吻升温（并肩版）— Agent6 教学检验 QA 报告

> **岗**：Agent6 教学检验 / **第四轮复检**（车间同事视角）  
> **初检 / 二轮 / 三轮**：2026-09-03 均 NO-GO（末轮阻塞项 **R16–R18**）  
> **第四轮复检日期**：2026-09-03  
> **权威手册**：[`goal-learning-center-copy-voice-warmth-plan-and-mega-prompt.md`](goal-learning-center-copy-voice-warmth-plan-and-mega-prompt.md)  
> **母版**：`imr` / `between_within_capability` / Wave-1  
> **读文对象**：`tools/learning_data/tutorial_overlays/*.json`（184）  
> **本岗**：只更新本报告；禁止改产品代码；model inherit  

---

## 第四轮总结论

| 项 | 结果 |
|----|------|
| **通过 / 驳回** | **通过** |
| **go / no-go** | **GO** |
| **可进 Agent5** | **是**（commit / push / 收口） |
| 金标 `imr` 回归 | **通过** |
| R16（OG 刷屏复制） | **通过**：cloned_unique1=0；og_len 典型 1～2（金标 3）；>8 = 0 |
| R17（点名骨架「本集在大约第 / 产线上对着」） | **通过**：命中 **0**；抽检可见 ≥4 套开场变体 |
| R18（万能尾刷屏） | **通过**：无整包复制刷屏；残句「先把看见的信号说完整」仍散见，**不挡 GO** |
| Python verify PASS | 已知（含 list / len≤8 / 去重） |
| 残余瑕疵 | 见 §6 风险行（变体壳、嵌套引号、个别 catalog 黑话）— **记录，不否决** |

**一句话**：第三轮否决点（OG 刷屏 + 点名骨架）已读文确认收口；金标仍像同事带看；Wave-2/3/4 抽检不再是「整库同一段」，而是分套变体换菜单名——**本 Goal 教学检验放行，可进 Agent5**。

---

## 1. 金标 `imr` 回归

| 硬点 / 口吻 | 状态 |
|-------------|------|
| 并肩口吻（SMT、不妨、无说教收束） | **通过** |
| 片 **41** / **55** | `buried_signals` + scenario / 读输出 **可指认** |
| **UCL ≠ USL** | glossary + I 图读法仍明示 |
| `dialog_fill_detail` | **9** 字段 |
| `output_guide` | **3 条互异**（I 图 / MR 图 / 点·诊断表） |
| 练习 7A/7C/7D/7E | 助教笔记风保留 |

**金标：通过。**

---

## 2. 指定课 `output_guide` 抽查（短且互异、可读）

| 课 | n | 互异？ | 读感 |
|----|---|--------|------|
| `msa_type1` | 2 | 是 | 菜单块 +「常见读法」；能指偏倚信号；略套，**可读、不刷屏** |
| `two_sample_t` | 2 | 是 | 同上；对上 B 线偏高；**通过 R16** |
| `histogram` | 2 | 是 | 右尾肩部可指；**通过** |
| `capability` | 2 | 是 | 偏心稳定集可指；**通过** |

全集扫描：`cloned_unique1=0`；`og_len` 分布约 **1:83 / 2:100 / 3:1**（金标）；**无 n>8**。

---

## 3. Wave-2 / 3 / 4 各 ≥3：是否还像「换菜单名同一段」？

### 点名骨架（R17）

| 串 | 命中 |
|----|------|
| `本集在大约第` | **0** |
| `产线上对着「` | **0** |

### 抽检读感（变体已分开）

| Wave | 抽检课 | 开场是否同一段？ |
|------|--------|------------------|
| W2 | `gage_rr` / `capability` / `pareto` / `msa_type1` | **否**：「当学员第一次点…」≠「用「…」摊开看一眼」≠「更像一张对照纸条」；scenario 亦分「还是那张本课练习表 / 导入后打开 / 这条练习线是为…」 |
| W3 | `two_sample_t` / `one_way_anova` / `regression` | **否**：助手邀请 / 学员第一次点 / 现场若要弄清… 三套不同 |
| W4 | `histogram` / `boxplot` / `scatter_plot` | **否**：摊开看一眼 / 对照纸条 / 助手邀请 交错 |

母版对照：`ewma`、`between_within_capability`、`imr` 仍明显深于批量变体——符合「金标+Wave-1 母版、批量课变体升温」的预期，**不构成再开 R17**。

残余：约 **49** 课仍共享「用「菜单」把这一类问题摊开看一眼」变体 A；属分套模板之一，**不如金标**，但已不是第三轮那种「整库一段 + OG×100」。

---

## 4. 练习区（附带）

上一轮点名占位（配套练习表 / 对话框：variables / 练习表 练习表）本轮抽检未见回归。`two_sample_t` 揭晓仍可读。**不挡 GO。**

---

## 5. DoD 勾选（第四轮）

- [x] 金标口吻 + 硬点回归  
- [x] R16：OG 去重、短、互异（读文 + 计数）  
- [x] R17：点名骨架清零；Wave 抽检有变体差  
- [x] R18：无刷屏万能尾（残句可接受）  
- [x] verify PASS（已知）  
- [x] 第四轮 QA 已写  
- [x] **Agent6 通过 → 可进 Agent5**  

---

## 6. 风险（一行）

**风险**：批量课仍有 4 套开场变体与嵌套引号/「说完整。，」标点毛刺（及偶发 `catalog：msa_type1_mode`）；不挡本 Goal 收口，建议 Agent5 后若再润色只定点扫变体 A 与标点，勿再大面积灌壳。

---

## 7. go / no-go

### **GO** — **可以进入 Agent5**

| 闸门 | 第四轮 |
|------|--------|
| 金标 | GO |
| R16 | GO |
| R17 | GO |
| R18 | GO |
| **Agent5（commit / push / §8 收口）** | **允许** |

提示用户本机 `package_dist` 抽查学习中心：金标 I-MR + 任一能力/推断/图形课口吻与读输出区（确认不再刷屏）。

---

## 8. 本岗变更

| 文件 | 动作 |
|------|------|
| `docs/research/learning-center-copy-voice-warmth-qa-report.md` | **覆盖更新**（第四轮 **GO**） |

未改 overlays / 生成器 / sqlite / C++。

---

## 9. 历史闸门摘要（便于 Agent5）

| 轮次 | 结论 | 主因 |
|------|------|------|
| 初检 | NO-GO | 旧黑名单壳大面积 |
| 二轮 | NO-GO | 新壳 + OG 字符串化 |
| 三轮 | NO-GO | OG 同句复制 61～153 遍（R16–R18） |
| **四轮** | **GO** | R16–R18 读文通过 |
