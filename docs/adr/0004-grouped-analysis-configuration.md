# ADR 0004：按分析族分组配置

- 状态：已接受
- 日期：2026-08-17

## 决策

`AnalysisConfiguration` 保留公共分析身份、数据选择和清洗字段；统计推断、控制图、时间序列、DOE、MSA、可靠性、功效和 PCA 参数分别放入嵌套配置对象。

项目 JSON 继续使用现有扁平键，以保持 `schema_version: 2` 的 `.dlab` 文件兼容。C++ 代码获得按领域族组织的接口，序列化层负责扁平键与嵌套对象之间的转换。
