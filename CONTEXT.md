# DataLab 领域词汇

## 产品边界

DataLab 是面向汽车制造业质量工程师的质量分析工具。它把质量数据、分析参数、分析结果和报告组织在一个可追溯的项目中。

## 核心术语

### 原始数据

从外部文件或数据源首次导入的数据快照。原始数据不可修改，也不因后续清洗而被覆盖。

### 分析数据

基于原始数据和已记录清洗操作生成的、供某次分析使用的数据视图。分析数据可以排除指定观测值，但必须能够追溯到原始数据。

### 清洗操作

对分析数据施加的、可记录和可复现的数据处理动作，例如类型识别、缺失值处理、单位转换和异常点排除。

### 异常点

被规则或用户识别为可能影响分析解释的观测值。异常点默认只标记，不从原始数据删除；是否排除由用户针对具体分析决定。

### 测量值

表示连续质量特性的数值列，例如尺寸、重量或扭矩。

### 子组

按照预先指定规则聚合的一组观测值，用于 Xbar-R 等子组控制图。子组规则必须在分析配置中明确。

### 不合格品

按照质量判定规则不满足要求的产品或单位。P 图需要不合格品数和检验总数，不能只使用一个笼统的计数列。

### 规格限

工程、客户或产品要求规定的允许边界，包括 LSL 和 USL。规格限描述“应该达到什么要求”，不是根据当前样本自动推导出的边界。

### 目标值

质量特性的名义值或期望值。目标值与样本均值不同；目标值只有在产品规范或用户明确提供时才存在。

### 过程均值

由实际分析数据计算出的平均水平。过程均值用于评价当前过程位置，不能替代规格目标值。

### 过程能力

在给定规格限和统计假设下，比较过程波动与规格范围的分析。Cp/Cpk 反映组内潜在能力，Pp/Ppk 反映总体过程表现。指数可附带置信区间（Cp/Pp 为 χ² 尺度，Cpk/Ppk 为 Bissell 近似）。

### 能力指数置信区间

能力指数点估计的区间。Cp/Pp 按 χ² 尺度伸缩；Cpk/Ppk 用 Bissell（1990）正态近似。缺规格一侧输出 `*`。区间描述抽样不确定性，不是合格判定。

### 拟合优度

检验分类观察频数是否符合一组指定比例（或等比例）。本产品用 Pearson χ²，命令为 `chi_square_gof`，与列联表独立性卡方不同。

### G 图

稀有事件的几何控制图。输入是已计算的非负间隔（天数或机会数），不是原始事件日期。

### T 图

稀有事件的 Weibull 控制图。输入同样是数值间隔；全部间隔大于 0 时用 Weibull MLE，含 0 间隔时排除后回归并给出诊断。

### 分析配置

一次分析所需的完整输入，包括数据字段映射、清洗视图、子组规则、规格限、目标值、统计方法和显示选项。

### 分析结果

由分析配置和分析数据确定的统计数值、图表序列、诊断信息、结论和算法版本。

### 项目

DataLab 中保存一次质量分析工作的完整载体，包括原始数据、分析数据、清洗记录、配置、结果和报告元数据。
